import { registerRootComponent } from 'expo';
import App from './App';

// Entry point for the Guilt Receipts Expo app.
// Registers the root App component so Expo can mount the UI.
registerRootComponent(App);
